//JavaScript
//JScript

function test()
{
 Log.Message("Generated random number: " + getRandomNumber(30,75))
}

function getRandomNumber(min, max)
{
  return Math.floor(Math.random() * (max - min + 1) ) + min;
}